import { UserIsInRoleDirective } from './user-is-in-role.directive';

describe('UserIsInRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new UserIsInRoleDirective();
    expect(directive).toBeTruthy();
  });
});
